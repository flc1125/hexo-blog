language: node_js

node_js:
  - 8

install:
  - npm install hexo-cli -g
  - npm install
  - echo -e "machine github.com\n  login ${GITHUB_TOKEN}" > ~/.netrc

script:
  - hexo deploy

branches:
  only:
    - master